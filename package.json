{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "node -e \"if(process.env.npm_config_user_agent && !process.env.npm_config_user_agent.includes('pnpm')) { console.warn('Warning: pnpm is recommended for this workspace.'); }\"",
    "build": "pnpm --filter frontend run build",
    "typecheck": "pnpm --filter frontend run typecheck"
  },
  "private": true,
  "devDependencies": {
    "typescript": "~5.9.2",
    "prettier": "^3.8.1"
  }
}
